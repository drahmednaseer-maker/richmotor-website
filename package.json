{
  "name": "richmotor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Rich Motor Company (RMC) — diesel generator manufacturer & power solutions provider, UAE",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node serve.mjs"
  },
  "engines": { "node": ">=18" }
}
